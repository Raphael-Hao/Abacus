syntax = "proto3";
package Server;

service Server { rpc SendQuery(Request) returns (Response); }

message Request {
  string model = 1;
  int32 bs = 2;
  int32 len = 3;
}

message Response { bool accepted = 1; }
